import React from "react";
import ReactDOM from 'react-dom'

ReactDOM.render(
  <React.StrictMode>    
    <p>This is going to be the routes page.</p> 
  </React.StrictMode>
  document.getElementById('root')
);